@{
    ViewData["Title"] = "Employee Dashboard";
}

<div class="container mt-4">
    <h2>Employee Dashboard</h2>

    <div class="mb-3">
        <a asp-action="AddFarmer" class="btn btn-primary">Add New Farmer</a>
        <a asp-action="ViewProducts" class="btn btn-info">View All Products</a>
        <a asp-action="Logout" class="btn btn-danger float-end">Logout</a>
    </div>

    <div class="card mt-4">
        <div class="card-body">
            <h5 class="card-title">Quick Actions</h5>
            <p class="card-text">Manage farmers and view product listings.</p>
        </div>
    </div>
</div>